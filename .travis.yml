language: node_js
node_js:
 - "stable"

services:
  - postgresql

before_script:
  - psql -c 'create database questioner;' -U postgres
  - psql -c "CREATE USER jaman WITH PASSWORD '123';" -U postgres
 addons:
  postgresql: "10"
  apt:
    packages:
    - postgresql-10
    - postgresql-client-10
 
after_success:
  - npm run coveralls
  - npm run code-climate
